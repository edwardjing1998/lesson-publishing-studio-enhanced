import test from 'node:test';import assert from 'node:assert/strict';import {activityCatalog,blobName,normalizeActivityMath,validateActivity,validateActivityRequest,validateSymbolicMath} from '../server/domain.mjs';
const endpoint='https://chat8gpteducationaccount.blob.core.windows.net';
test('ABFSS resolves safely',()=>assert.equal(blobName('abfss://education@chat8gpteducationaccount.dfs.core.windows.net/generated/x.png',endpoint,'education'),'generated/x.png'));
test('catalog contains recommended math types',()=>{const types=new Set(activityCatalog().map(x=>x.type));for(const x of ['THEOREM_PROOF','MULTIPLE_CHOICE','PROOF_EXERCISE','DIAGRAM_EXPLORATION'])assert.ok(types.has(x));});
test('request bounds variants and process IDs',()=>{const ids=new Set(['p1']);assert.equal(validateActivityRequest({selections:[{process_id:'p1',activity_type:'CALCULATION',variants:2}]},ids).selections.length,1);assert.throws(()=>validateActivityRequest({selections:[{process_id:'p1',activity_type:'CALCULATION',variants:4}]},ids));});
test('activity preserves references and validates MCQ',()=>{const process={process_id:'p1',prerequisites:[{prerequisite_id:'r1'}]},activity={status:'SUCCEEDED',activity_type:'MULTIPLE_CHOICE',process_id:'p1',title:'Angles',objective:'Classify',instructions_markdown:'Choose.',blocks:[{block_id:'q',kind:'QUESTION',markdown:'What is ∠A?'}],choices:[{choice_id:'A',text:'10°'},{choice_id:'B',text:'20°'},{choice_id:'C',text:'30°'}],correct_choice_id:'B',solution_markdown:'∠A = 20°.',prerequisite_ids:['r1'],image_ids:['i1']};assert.equal(validateActivity(activity,{type:'MULTIPLE_CHOICE',process,imageIds:['i1']}),activity);assert.equal(validateSymbolicMath(activity,{process,images:[{id:'i1'}]}),true);});
test('common LaTeX angle notation is normalized to visible symbols',()=>{const value=normalizeActivityMath({text:'\\angle A + \\angle B = 180^{\\circ} and 2 \\times 3'});assert.equal(value.text,'∠A + ∠B = 180° and 2 × 3');});
